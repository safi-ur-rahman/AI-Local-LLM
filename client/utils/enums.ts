export enum NodeEnvironment {
    Development = 'development',
    Production = 'production',
    Test = 'test',
}

export enum MessageUsersTypes {
    System = 'system',
    User = 'user',
    Assistant = 'assistant',
}